import styles from "./LoginForm.module.css";

const Message = ({
  activeLogin,
  setActiveLogin,
  activeRegister,
  setActiveRegister,
  activeMessage,
  setActiveMessage,
}) => {
  return (
    <div>
      Message
      <button>Login</button>
    </div>
  );
};

export default Message;
